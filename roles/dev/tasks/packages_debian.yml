- name: Add apt repository
  apt_repository:
    repo: "ppa:fish-shell/release-2"
  become: yes

- name: Install common packages
  apt:
    name: '{{ item }}'
    state: present
  become: yes
  with_items: ['{{ packages }}']
